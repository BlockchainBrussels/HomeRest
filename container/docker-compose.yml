version: '3'

services:

  lightbo.lt:
    image: lightbo.lt-base:latest
    container_name: lihtbo.lt-python
    ports:
      - 13000:3000
    networks:
      - lightbo.lt-network
    depends_on:
      - mariadb

  mariadb:
    image: lightbo.lt-db:latest
    container_name: lightbo.lt-db
    restart: always
    ports:
      - "13306:3306"
    volumes:
      - mariadb-data:/var/lib/mysql
    networks:
      - lightbo.lt-network
    environment:
      MYSQL_ROOT_HOST: '%'
      MYSQL_ROOT_PASSWORD: 'homereset'
      MYSQL_USER: 'homereset'
      MYSQL_PASSWORD: 'quertyhomerest'
      MYSQL_DATABASE: 'homerest'

volumes:
  mariadb-data:

networks:
  lightbo.lt-network:
